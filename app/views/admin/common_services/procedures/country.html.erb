<% content_for :title, t('admin.common_services.procedures.country.title',
      procedure: named_procedure(@procedure.code), country: named_country(@country)) %>

<%= render DirectoryBreadcrumbsComponent.new(trail: [
      [t('admin.common_services.procedures.index.title'), admin_common_services_procedures_path],
      [@procedure.code, admin_common_services_procedure_path(@procedure.code)],
      [named_country(@country), nil],
    ]) %>

<%= render 'admin/common_services/declarations' %>
