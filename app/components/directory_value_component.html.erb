<span class="directory-value" lang="<%= lang %>" title="<%= label %>">
  <%# The tint alone would say it to the eye only, which RGAA 3.1 refuses. The
      wording is read once, off screen, and rides the tooltip for whoever
      hovers it. %>
  <span class="fr-sr-only"><%= label %></span>
  <%= content %>
</span>
